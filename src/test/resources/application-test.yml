debug: false

JWT_SECRET: XBlci1zZWNyZXQta2V5LXN1cGVyLXNlY3JldC1rZXk=c3VwZXItc2VjcmV0LWtleS1zd
JWT_EXPIRE: 36000000

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ecommercetest}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Masoko}

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        c3p0:
          timeout: 3

  flyway:
    community-db-support-enabled: false

logging:
  level:
    root: INFO
    org.springframework.boot.autoconfigure: INFO
    org.springframework.context.annotation: WARN
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
